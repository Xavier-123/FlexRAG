"""
Unit tests for FlexRAG.

These tests use only standard-library / already-installed packages plus
``pytest``.  All external service calls (OpenAI, vLLM) are replaced with
lightweight stubs so the suite runs offline.
"""
